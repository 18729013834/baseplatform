@using base_platform.Web.HtmlExtensions
@using base_platform.Web.Models
<!--add customized code between this region-->
<!--add customized code between this region-->

@model SearchModel
<div class="search-container">
    @using (Html.BeginForm("Search", "sys_resource", FormMethod.Post, new {data_ajax = "true", data_ajax_method = "POST", data_ajax_mode = "replace", data_ajax_update = "#sys_resource_DataList", name = "sys_resource_Search", id = "sys_resource_Search"}))
    {
        @Html.AntiForgeryToken()
        @Html.TextBoxFor(m => m.Name, new {@class = "form-control", placeholder ="name"})
        @Html.HiddenFor(m => m.Pager.PageSize)

        <input class="m_btn" type="submit" value="Search"/>
    }
</div>

